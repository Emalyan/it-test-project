export interface Movie {
  id: string;
  name: string;
  image: string;
  countries: string;
  vote: string;
  count_vote: string;
  actors: string;
  order: number;
}

export interface CardPosition {
  id: string;
  order: number;
}
