import styled from 'styled-components';

const MoviesBoard = styled.div`
    display: flex;
    flex-wrap: wrap;
    font-size: 20px;
    font-style: italic;
    justify-content: center;
`;

export const Styled = { MoviesBoard }